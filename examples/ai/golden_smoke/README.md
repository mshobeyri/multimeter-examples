# Golden smoke (AI)

Tiny API + scaffolded test used as the default Multimeter few-shot for agents.

- `apis/echo.mmt` — source API
- `tests/echo-smoke.mmt` — must match `scaffold_test` output

See `docs/AI/golden-smoke.md`.
