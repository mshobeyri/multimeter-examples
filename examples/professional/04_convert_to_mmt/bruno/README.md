# Multimeter Test Server — Bruno

Bruno collection for [test.mmt.dev](https://test.mmt.dev). Open `bruno.json` with **Open as MMT** for the full collection, or open any `.bru` for that request alone. Convert any `.bru` with **Convert to MMT...**.

- Collection: `bruno.json` + `*.bru`
- Converted samples: `converted/api/` and `converted/tests/` (one pair per request file)

See the [professional convert overview](../README.md). For a one-request sample, see [intermediate Bruno convert](../../../intermediate/25_convert_to_mmt/bruno/).
