# HTTP convert (basic)

`source.http` is a one-request HTTP file. **Convert to MMT...** produces an API file and a test that calls it under `converted/`.

See the [convert overview](../README.md) for the other formats.
